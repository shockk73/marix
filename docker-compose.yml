services:
  bot:
    build: .
    container_name: marik-check
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/watches.db
    volumes:
      - bot-data:/data

volumes:
  bot-data:
